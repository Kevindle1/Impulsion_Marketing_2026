{
  "name": "impulsion-marketing",
  "version": "2.0.0",
  "description": "Outil interne de pilotage des campagnes marketing — application 100% client (File System Access API). Aucune dépendance runtime.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-bundle.mjs",
    "test": "node --test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "eslint-plugin-html": "^8.1.4"
  }
}
